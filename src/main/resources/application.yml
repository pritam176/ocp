
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

server:
  forward-headers-strategy: FRAMEWORK
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.30.173.71:3306/sampledb
    username: IS_UAEPASS_INTEGRATION
    password: ENC(fqBQJQrG8GTLZOPhmsaKxKMWfBsiIZOa7M/tqveIUCA=)
  jpa:
    hibernate.ddl-auto: create
